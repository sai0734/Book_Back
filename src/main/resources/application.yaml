spring:
  application:
    name: practice
  datasource:
    url: jdbc:mysql://localhost:13306/myapp?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: info
    com.book.practice: debug
server:
  port: 8080