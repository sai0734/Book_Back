spring:
  application:
    name: practice
  datasource:
    url: jdbc:mysql://localhost:13306/myapp?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: root
  mail:
    host: smtp.naver.com
    port: 587
    username: "sinkijun890@naver.com"
    password: "WGSNTQXVWN5X"
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
      mail.smtp.ssl.trust: smtp.naver.com
      mail.debug: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: info
    com.book.practice: debug
server:
  port: 8080